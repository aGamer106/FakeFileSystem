#include "Directory.h"
#include <iostream>

using namespace std;

//Directory::Directory(char* path, char* name, double size, char timeOfEdit) : path(path), name(name), size(size), timeOfEdit(timeOfEdit)
//{
//
//}